(ns clojure-luminus-spike.service.user-service
  (:require [clojure-luminus-spike.db.core :as db]))

;; (defn get-users
;;   ([] (db/list-user))
;;   ([id] (db/get-user id)))

;; (defn get-item
;;   ([] (db/list-item))
;;   ([] (db/list-item id)))
